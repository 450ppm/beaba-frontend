import './TempCard.css';

/* TempCard is now unused — sensor display is handled by SensorList.
   Keeping as a minimal stub for backwards compatibility. */
export default function TempCard({ sensor }) {
  return null;
}
